# Copy to .env and set values. For hosting, set these in your platform's environment.

# Required in production: use a long random string
# SECRET_KEY=your-secret-key-here

# Optional: use a remote database instead of SQLite
# Leave unset for local SQLite (database/app.db)

# PostgreSQL (e.g. Railway, Render, Heroku)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# MySQL (e.g. some shared hosting)
# DATABASE_URL=mysql+pymysql://user:password@host:3306/dbname

# Server port (many hosts set PORT automatically)
# PORT=5000

# Set to 1 for debug mode (do not use in production)
# FLASK_DEBUG=0

# Optional: send welcome email to users on registration (leave unset to skip)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=1
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password
# MAIL_DEFAULT_SENDER=your-email@gmail.com
